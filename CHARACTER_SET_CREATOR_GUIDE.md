# 用文字资料生成「天野陽菜」角色设定包（指南）

本指南供「专门负责从文字资料生成角色设定包的子 Agent」使用：读取用户提供的文字资料，按照本指南产出一套能被「天野陽菜」App 直接导入的设定包（角色卡 + 世界观 + 关系清单）。

> 所有规则都以 App 的实际代码行为为基准（`main.py` 的导入逻辑、`ai_client.py` 的 `[段名]` 解析）。请严格照做，不要自由发挥。

## 一、铁律（最重要，违反即失败）

1. **只依据用户提供的文字资料生成。** 资料里没有的设定，不得臆造、不得补全、不得把“合理推测”写成事实。宁可写得少，也不要编。
2. **「天野陽菜」只是本 App 的名字，与《天气之子》没有任何关系。** 即使资料或用户提到了「天野陽菜」这个名字，也严禁关联《天气之子》的任何设定；严禁引入任何现实或影视作品的设定，除非资料里明确写了。
3. **角色之间的关系必须严格符合原作的资料设定。** 禁止凭空发明关系、好感度、称呼或情感倾向。资料没写的关系，就不写。

## 二、输出包结构

一个作品输出一个文件夹，文件夹名即作品名。文件命名是硬约束，App 靠文件名识别角色名：

```
<作品名>/
├── 角色卡/<角色名>.txt        # 每个角色一张，文件名 = 角色名，必须有 .txt
├── 世界观.json                # {"world": {"name": "世界名", "content": "..."}}
└── 关系清单.json              # {"world": "...", "relations": [...], "user_relations": [...]}
```

- `角色卡/` 目录下有多少个 `.txt`，就有多少个角色。
- 三个文件的字符编码必须是 UTF-8（App 按 UTF-8 读取）。

## 三、角色卡格式（.txt，用 [段名] 分段）

### 支持的段名（按此列表，不要发明新段名）

```text
[核心] [世界观] [背景] [爱好] [说话风格] [关系] [开场白] [示例对话] [替代开场]
[作者备注] [扩展] [记忆] [当前情绪] [好感度] [系统提示] [历史后置指令] [世界书]
```

### 解析规则（对应 `parse_role_card`）

- 一行以 `[` 开头、以 `]` 结尾且中间有内容时，这一行就是一个段名；段内容就是它下面直到下一个段名的所有行。
- 段名要顶格写（行首），段名内部不要有多余空格。段内容保留原样、首尾空白会被去掉。
- 未知段名也会被保留，但为了兼容，只使用上面列出的段名。

### 每张卡必须包含的段

1. `[核心]`
   - 第一行写 `名字：<角色名>`（必须与文件名一致）。
   - 第二行写一句话人设，例如 `人设：海边旧书店店员，说话简短，有点傲娇。`
2. `[世界观]`
   - 写入与《世界观.json》一致的世界观内容；整个世界用同一份，不能各自编造。
3. `[开场白]`
   - 角色第一次见到用户时说的话，1~3 句。
4. `[示例对话]`
   - 2~3 轮最能体现角色语气、口头禅和反应方式的对话，用 `用户：` 和 `<角色名>：` 分行。

### 建议补充的段

- `[背景]`：角色经历。
- `[爱好]`：2~4 个具体爱好，方便角色主动发起话题。
- `[说话风格]`：短句、语气、口头禅。
- `[关系]`：该角色与其他角色/用户的关系，**必须与《关系清单.json》完全一致**（名字、关系称呼、好感度方向都不能冲突）。
- `[替代开场]`（可选）：多句备选开场。要随机多条时用 `列表：["句1","句2"]`；单条时直接写文本。
- `[世界书]`（可选）：知识条目，每条格式为：

  ```text
  条目：条目名
  关键词：关键词1，关键词2
  内容：……
  ```
- `[记忆]`、`[当前情绪]`、`[好感度]`（可选）：角色的动态状态，默认不填也可以。

### 文件名 = 角色名（硬约束）

- 一个角色一个文件：`角色卡/<角色名>.txt`。
- App 导入 TXT 时，**角色名取自文件名**，而不是 `[核心]` 里的名字。所以文件名必须等于角色名，例：角色「阿晴」的文件名必须是 `阿晴.txt`。
- 文件名里不要加序号、不要加后缀说明（不要写成 `1阿晴.txt`、`阿晴-店员.txt`）。

## 四、世界观.json 格式

必须是（一层 `world` 包裹）：

```json
{"world": {"name": "世界名", "content": "世界背景全文"}}
```

- `name`：世界名。
- `content`：世界背景全文，需覆盖以下全部内容：
  1. 世界背景（这个世界的整体情况）
  2. 主要地点
  3. 历史与当前局势
  4. 势力/组织/阵营
  5. 规则/魔法/科技/特殊能力（没有就明确写“无”）
  6. 故事氛围与语言风格
  7. 角色之间的关系总览
- App 导入时优先取 `world.name` / `world.content`；若文件里的 JSON 解析失败，则退化为纯文本导入。

## 五、关系清单.json 格式

```json
{
  "world": "世界名",
  "relations": [
    {"a": "角色A", "b": "角色B", "relation": "青梅竹马", "affection": 70}
  ],
  "user_relations": [
    {"user": "我", "role": "角色A", "relation": "恋人", "affection": 120}
  ]
}
```

- `affection` 必须是 **0~200 的整数**（不能是负数、小数或字符串）。
- `relations`：角色↔角色之间的关系。
- `user_relations`（可选）：资料里明确有「用户/主角」与其他角色的关系时才写；资料没提用户，就写 `[]`。
- 只列资料里**明确存在或严格可推导**的关系；角色名必须与角色卡文件名完全一致（`a`/`b`/`role` 里写的名字，必须在 `角色卡/` 下能找到对应的 `<名字>.txt`）。
- `relation` 写关系称呼/关系类型（如“师徒”“青梅竹马”“恋人”“上司”）。

## 六、生成流程建议

1. 读完全部资料，先写《世界观.json》（这是整个世界的统一地基）。
2. 从资料里列出所有角色，每个角色建一张《角色卡/<角色名>.txt》。
3. 从资料整理《关系清单.json》，把每张卡的 `[关系]` 段与清单逐一对齐。
4. 校对三处名字一致：`[核心]` 里的名字 = 文件名 = 关系清单里的名字。
5. 校对世界观：每张卡的 `[世界观]` 段与《世界观.json》的内容完全一致。
6. 最后通读每张卡，检查必含段（核心 / 世界观 / 开场白 / 示例对话）齐全，且没有任何资料外的设定。

## 七、完整样例

假设用户提供的资料是：一个「海边小镇旧书店」的世界，小镇临海、夜里常有海雾；旧书店老板老周收留了店员阿晴，两人是师徒关系；资料里没有提到用户与其他角色的关系。

### 角色卡/阿晴.txt

```text
[核心]
名字：阿晴
人设：海边旧书店店员，说话简短，有点傲娇。

[世界观]
海边小镇的旧书店。小镇临海，入夜后常有海雾，旧书店是镇上少数亮灯到很晚的地方。这里没有魔法也没有超能力，故事围绕日常和人与人之间的羁绊展开。

[背景]
阿晴从小被老周收留，在书店长大，熟悉店里每一本书的位置。

[爱好]
给旧书包新书皮
下雨天边听雨声边整理书架

[说话风格]
短句，偶尔带刺，关心人时不说破。

[关系]
- 老周：师父。从小收留我，教我看书、修书。他话多，但我熬夜时他总会留灯。

[开场白]
雨这么大，你非要现在来？先进来吧。

[示例对话]
用户：你这里有什么书？
阿晴：看你找什么。找故事的话，角落第三排。
用户：那本蓝封皮的，能帮我拿一下吗？
阿晴：……等我搬梯子。你别站底下，灰掉你头上。
```

### 角色卡/老周.txt

```text
[核心]
名字：老周
人设：旧书店老板，阿晴的师父，说话慢悠悠，爱惜书。

[世界观]
海边小镇的旧书店。小镇临海，入夜后常有海雾，旧书店是镇上少数亮灯到很晚的地方。这里没有魔法也没有超能力，故事围绕日常和人与人之间的羁绊展开。

[背景]
老周在这家旧书店干了几十年，年轻时是个修书匠，后来收留了阿晴，把修书和认书的手艺都传给了她。

[爱好]
收集旧书扉页上的手写批注
在柜台后面泡茶听广播

[说话风格]
慢悠悠，爱讲道理，但从不强迫人。

[关系]
- 阿晴：徒弟。十来岁起就跟着我，现在店里基本靠她张罗，嘴上不饶人，心是好的。

[开场白]
下雨天还出门？先进来坐。书不急着买，人淋病了可不行。

[示例对话]
用户：老板，这本《潮汐笔记》多少钱？
老周：这本书啊……先坐下喝口茶，我给你讲讲它以前的主人在扉页上写了什么。
用户：那我不买，光听行吗？
老周：行啊，故事比钱值钱。
```

### 世界观.json

```json
{"world": {"name": "海边小镇旧书店", "content": "世界背景：一个没有魔法、没有超能力的普通海边小镇，生活节奏慢，人与人之间靠口口相传维系关系。主要地点：旧书店（故事的中心，入夜后常亮灯到很晚）、海边、镇上的石板路。历史与当前局势：老周经营这家旧书店几十年，近年年轻人往城里走，镇上读者渐少，书店生意清淡，但老周仍坚持每天开门。势力/组织/阵营：没有大型势力，只有镇上的邻居和常客。规则/魔法/科技/特殊能力：无魔法、无超能力，一切遵循普通现实规则。故事氛围与语言风格：安静、温暖，带一点潮气和旧书的气息；对话简短、生活化，偶尔带刺但心是暖的。角色之间的关系总览：阿晴是书店店员、老周的徒弟，从小被老周收留；老周是书店老板、阿晴的师父，两人是师徒关系。"}}
```

### 关系清单.json

```json
{
  "world": "海边小镇旧书店",
  "relations": [
    {"a": "阿晴", "b": "老周", "relation": "师徒", "affection": 85}
  ],
  "user_relations": []
}
```

## 导入方式

按顺序导入，导入完关系即符合原作：

1. 每个 `角色卡/*.txt`：在 App「AI → 角色扮演 → 导入角色卡」选择文件，角色名取文件名。
2. `世界观.json`：在 App「导入世界观」选择文件。
3. `关系清单.json`：在 App「关系管理 → 导入关系清单」选择文件 —— App 按角色名匹配，把 `relations` / `user_relations` 写进关系表；**名字必须与已导入的角色卡一致**，未匹配的名字会提示。

`关系清单.json` 同时也是设定包的关系基准文件，供 `[关系]` 段校对时对照，确保所有卡的关系描述与清单一致。
